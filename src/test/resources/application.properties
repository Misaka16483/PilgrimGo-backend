spring.application.name=pilgrimgo-test

# Datasource — duplicated from main/application.properties because a test-classpath
# application.properties fully shadows the main one (Spring does not merge them).
spring.datasource.url=jdbc:postgresql://8.130.160.51:5432/BJTU2026
spring.datasource.username=BJTU2026
spring.datasource.password=test-only-database-password
spring.datasource.driver-class-name=org.postgresql.Driver

# JWT — used by JwtUtil if it ever gets instantiated (most tests mock it out).
jwt.secret=test-only-jwt-signing-key-32-bytes-minimum
jwt.expiration=3600000
